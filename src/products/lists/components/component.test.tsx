import {afterEach, test, describe} from "vitest";
import {cleanup} from "@testing-library/react";

describe('UI component tests', () => {
    afterEach(() => {
        cleanup()
    })

    test('desc', () => {
        // Arrange
        // Act
        // Assert
    })
})